{
  "name": "moo-meter-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "dev:docs": "npm --prefix backend run docs:serve",
    "dev:all": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:docs\"",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "lint": "npm --prefix frontend run lint",
    "test": "npm --prefix backend run test && npm --prefix frontend run test",
    "docs": "npm --prefix backend run docs",
    "docs:serve": "npm --prefix backend run docs:serve"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
